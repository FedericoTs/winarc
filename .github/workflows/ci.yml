name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  domain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @arc/domain typecheck
      - run: pnpm --filter @arc/domain test
      - run: pnpm --filter @arc/eval-verification typecheck
